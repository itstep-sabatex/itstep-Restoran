<UserControl x:Class="RestoranClient.LoginOld"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RestoranClient"
        mc:Ignorable="d"
        Width="300" Loaded="Window_Loaded">
    <Grid >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Label >Login</Label>
        <Label Grid.Row="1" Margin="0,5,0,0">Password</Label>
        <ComboBox Grid.Column="1" ItemsSource="{Binding Path=Waiters}" DisplayMemberPath="name" SelectedValuePath="id" SelectedValue="{Binding Path=WaiterId}"/>
        <TextBox Grid.Row="1" Grid.Column="1" Margin="0,5,0,0" Text="{Binding Path=Password}" />


        <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal"  HorizontalAlignment="Right">
            <Button Click="Button_Click" Width="50" Margin="5">Ok</Button>
            <Button Click="Button_Click_1" Width="50" Margin="5">Cancel</Button>
        </StackPanel>
        
        
    </Grid>
</UserControl>
